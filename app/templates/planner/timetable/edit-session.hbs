<h1>Edit Timetable Session</h1>
<h1>{{model.group.name}}</h1>

{{!-- Day and Time controls at top of modal --}}
{{#forms/form-row label="Day"}}
    <p>{{model.day}}</p>
{{/forms/form-row}}
{{#forms/form-row label="Time"}}
    <p>{{model.time}}</p>

{{/forms/form-row}}

{{!-- Group Selection --}}
{{#forms/form-row label="Group"}}
<div class="row">
    {{!-- {{#owl-carousel}} --}}
    {{#each groups as |group|}}
        <div class="col-sm">
            {{icons/group-icon group=group}}
            {{!-- <div class="card">
                <img class="card-img-top img-thumbnail" src="/assets/images/kids1.png" />
                <div class="card-footer">{{group.name}}</div>
            </div> --}}
        </div>
    {{!-- <button class="btn btn-primary" {{action 'setSelectedGroup' group.name}}>{{group.name}}</button> --}}
    {{/each}}
    {{!-- {{/owl-carousel}} --}}
</div>
{{/forms/form-row}}

{{#forms/form-row label="Delete"}}
    <button class="btn btn-warning" {{action 'deleteSession'}}>Delete</button>
    {{!-- {{#modal-basic}}
        Permanently delete this session?
    {{/modal-basic}} --}}

{{/forms/form-row}}


    <button class="btn btn-danger" {{action 'cancelEditSession'}}>Cancel</button>
    <button class="btn btn-success" {{action 'saveSession'}}>Save</button>


{{outlet}}
