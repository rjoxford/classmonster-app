{{#bread-crumb link='objectives.objective' model=model}}{{model.name}}{{/bread-crumb}}

<div class="card">
    <div class="card-block">
        <h1>{{model.name}}</h1>
    </div>
    <div class="card-block">
        <button class="btn btn-success" type="button">Maths</button>
        <button class="btn btn-warning" type="button">  8 / B  </button>
        <button class="btn btn-success" type="button">Maths</button>
    </div>
</div>
{{outlet}}
